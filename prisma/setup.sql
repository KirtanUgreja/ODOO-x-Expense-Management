-- PostgreSQL Database Setup for ExpenseFlow
-- Run this script to set up the database manually if needed

-- Create database (run as postgres user)
-- CREATE DATABASE expenseflow;

-- Grant permissions (optional, for production)
-- GRANT ALL PRIVILEGES ON DATABASE expenseflow TO your_user;
